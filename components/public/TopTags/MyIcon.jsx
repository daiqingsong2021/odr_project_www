import { Menu, Dropdown, Icon, Spin, Tooltip } from 'antd';
 const MyIcon = Icon.createFromIconfontCN({
    scriptUrl: '/static/fonts/icon1/iconfont.js', // 在 iconfont.cn 上生成
  });
  export default MyIcon